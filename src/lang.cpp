#include <iostream>
#include <string>
using namespace std;

int main()
{
    bool useLang1, useLang2 = false;
    bool goodLang1, goodLang2 = true;

    string input;

    getline(cin,input);

    if(input[0] == 'a')
        useLang1 = true;
    if(input[0] == 'b')
        useLang2 = true;

    if(useLang1)
    {
        while(goodLang1)
        {
            
        }
    }

    return 0;
}